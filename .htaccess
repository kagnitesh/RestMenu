RewriteEngine On
RewriteBase /

# If the request is not an existing file or directory, redirect to site root
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^.*$ / [R=302,L]


